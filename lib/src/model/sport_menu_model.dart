import 'dart:ui';

class SportMenuModel {
  Color backgroundColor;
  Color textColor;
  double width;
  double height;
  String text;
  SportMenuModel({required this.backgroundColor,required this.textColor,required this.width,required this.height,required this.text});
}